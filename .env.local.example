# Copy to .env.local and fill in from your Supabase project (Settings -> API).
NEXT_PUBLIC_SUPABASE_URL=""
NEXT_PUBLIC_SUPABASE_ANON_KEY=""

# Server-only secret (NEVER prefix with NEXT_PUBLIC). Used for SharePoint sync
# and other server-side jobs later. Leave blank for now.
SUPABASE_SERVICE_ROLE_KEY=""

# Microsoft 365 / SharePoint (added when we build the sync — leave blank for now)
MS_TENANT_ID=""
MS_CLIENT_ID=""
MS_CLIENT_SECRET=""
SHAREPOINT_SITE_URL=""
